{
  "name": "analytics-engine",
  "private": true,
  "workspaces": [
    "core",
    "knex",
    "pg",
    "browser",
    "graphql",
    "benchmarks",
    "compat"
  ],
  "scripts": {
    "test": "pnpm -r test --filter='!compat'",
    "build": "pnpm -r build"
  }
}
